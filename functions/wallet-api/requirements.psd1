@{
    # Aucun module externe requis : Invoke-RestMethod (Graph, Pass2U) et les bindings
    # Table Storage natifs suffisent. Décommenter si Connect-AzAccount (profile.ps1) est
    # réellement utilisé un jour.
    # 'Az.Accounts' = '2.*'
}
